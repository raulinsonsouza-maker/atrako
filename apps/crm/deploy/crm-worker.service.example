[Unit]
Description=CRM BullMQ Worker
After=network.target redis.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/crm
EnvironmentFile=/var/www/crm/.env
ExecStart=/usr/bin/node jobs/worker.js
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
